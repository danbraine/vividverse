{
  "name": "vividverse",
  "version": "0.1.0",
  "description": "VividVerse - AI-Powered Film Generation Platform",
  "scripts": {
    "dev": "echo 'Start backend: cd backend && npm run dev' && echo 'Start frontend: cd src/coverce_frontend && npm run dev'",
    "install:all": "npm install && cd backend && npm install && cd ../src/coverce_frontend && npm install && cd ../ai_orchestrator && npm install"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}



